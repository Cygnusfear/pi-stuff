---
id: p-78f3
status: in_progress
deps: []
links: []
created: 2026-02-23T05:48:23Z
type: bug
priority: 2
assignee: Alexander Mangel
tags: [bug, mcp, pi]
---
# memory_unfold MCP tool returns JSON parse error on direct tool call

memory_unfold tool call returns `TotalRecall error: JSON Parse error: Unrecognized token ''` when called via direct tool invocation, but works fine via execute_code/memoryUnfold(). Likely a pi MCP client issue — response parsing or empty string handling on the MCP transport layer.



## Goal
memory_unfold tool call returns `TotalRecall error: JSON Parse error: Unrecognized token ''` when called via direct tool invocation, but works fine via execute_code/memoryUnfold(). Likely a pi MCP client issue — response parsing or empty string handling on the MCP transport layer.

## Acceptance Criteria
- [ ] TODO

## Verification
- [ ] TODO

## Worktree
- .
